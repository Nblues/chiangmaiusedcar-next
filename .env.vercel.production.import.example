# Vercel Production import template (SAFE)
# ใช้ไฟล์นี้สำหรับ Import เข้า Vercel เฉพาะค่าที่จำเป็น
# 1) ใส่ค่าจริงแทน <...>
# 2) Import ใน Vercel -> Project Settings -> Environment Variables -> Import .env
# 3) เลือก Environment = Production แล้ว Redeploy

# Shopify Storefront (ต้องมีอยู่แล้วบน Vercel โปรดเช็คให้ถูกต้อง)
SHOPIFY_DOMAIN="kn-goodcar.com"
SHOPIFY_STOREFRONT_TOKEN="<YOUR_STOREFRONT_TOKEN>"
SHOPIFY_API_VERSION="2024-01"

# Shopify Admin fallback (สำหรับดึง spec ที่เป็น metaobject_reference / data ที่ Storefront อ่านไม่ได้)
SHOPIFY_ADMIN_DOMAIN="kn-goodcar.myshopify.com"
SHOPIFY_ADMIN_TOKEN="<YOUR_ADMIN_TOKEN>"
# (optional)

# Secure debug endpoint gate (เพื่อเปิด /api/debug/shopify-admin-status-secure)
DEBUG_ENDPOINT_SECRET=<YOUR_DEBUG_ENDPOINT_SECRET>

# Site
SITE_URL=https://www.chiangmaiusedcar.com
