# 🚨 VERCEL ENV VARIABLES - ต้องตั้งค่าก่อน Deploy

## ✅ แนวทางมาตรฐานสากล (Security)

- ห้ามใส่รหัสผ่าน/โทเคนจริงลงในไฟล์ใน repo
- ตั้งค่า env ผ่าน Vercel Dashboard หรือสคริปต์ที่ prompt รับค่า (ซ่อนไว้)
- ถ้าเคยแชร์/commit secret ไปแล้ว ให้ rotate ค่าทันที

## ✅ ตั้งค่าที่ Vercel (5 นาที)

### 1) เข้า Vercel
https://vercel.com/dashboard → Project → Settings → Environment Variables

### 2) เพิ่มตัวแปรขั้นต่ำสำหรับ admin

| Name | Value | Environments |
|------|-------|--------------|
| `ADMIN_USERNAME` | (your username) | ✅ All (Prod+Preview+Dev) |
| `ADMIN_PASSWORD` | (strong password) | ✅ All (Prod+Preview+Dev) |
| `SESSION_SECRET` | (random 32+ chars) | ✅ All (Prod+Preview+Dev) |

แนะนำสร้าง `SESSION_SECRET` ด้วย OpenSSL:
- `openssl rand -hex 32`

### 3) Redeploy

Deployments → latest → ⋮ → Redeploy (ไม่ใช้ cache)

### 4) Test

https://www.chiangmaiusedcar.com/admin/login

---

📄 ตัวอย่างค่าที่ใช้ในเครื่อง local ดูได้ที่ `.env.example`
🔍 Check status: `node scripts/check-vercel-env.mjs`
