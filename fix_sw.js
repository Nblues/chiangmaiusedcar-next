const fs = require('fs'); let code = fs.readFileSync('public/sw.js', 'utf8'); code = code.replace(/v2026-1\.0\.3/g, 'v2026-1.0.4'); code = code.replace('  // Skip Google Fonts - let browser handle it directly', '  // Skip Google Analytics & Tag Manager\n  if (hostname.includes(\'google-analytics.com\') || hostname.includes(\'googletagmanager.com\')) {\n    return;\n  }\n\n  // Skip Google Fonts - let browser handle it directly'); fs.writeFileSync('public/sw.js', code);
